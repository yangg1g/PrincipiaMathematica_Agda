---
description: Agda proof format conventions for Principia Mathematica
globs: "**/*.agda"
alwaysApply: false
---

# Agda 证明格式规范

本规则统一 Principia Mathematica 项目中的证明书写风格。

## 基本原则

1. **缩进**：统一使用 2 空格
2. **行宽**：单行证明、类型声明与 `--` 注释均建议 ≤ 80 字符（避免 >100 字符的行长）
3. **空行**：定理之间保留一个空行，分隔线 `--------` 与定理之间不空行

## 证明格式

### 单行证明

当证明项较短（约 ≤ 80 字符）时，使用单行：

```agda
*4·2 : ⊢ (p ≡ p)
*4·2 = Adj Id Id

*4·13 : ⊢ (p ≡ ~ (~ p))
*4·13 = Adj *2·12 *2·14
```

### 多行证明

当证明项较长时，换行并缩进 2 空格：

```agda
*4·38 : ⊢ (((p ≡ r) ∙ (q ≡ s)) ⊃ ((p ∙ q) ≡ (r ∙ s)))
*4·38 =
  Comp (Syll (Prael *3·26 *3·26) *3·47)
       (Syll (Prael *3·27 *3·27) *3·47)
```

- `=` 后换行，证明体缩进 2 空格
- 二元运算符或函数的两个参数：若换行，对齐到开括号或上一参数

### 等值链（Equational Reasoning）

证明 `p ≡ q` 时，优先使用 `_≡⟨_⟩_` 和 `_∎`：

```agda
*4·42 : ⊢ (p ≡ ((p ∙ q) ∨ (p ∙ ~ q)))
*4·42 =
  _ ≡⟨ Adj (MP *2·11 *3·21) *3·26 ⟩
  _ ≡⟨ *4·4 ⟩
  _ ∎
```

- 每步单独一行
- `≡⟨` 与 `⟩` 之间为证明项
- 两式左端相同（`⊢ (y ≡ x)` 与 `⊢ (y ≡ z)`）要得到 `⊢ (x ≡ z)` 时，用 `_≡˘⟨_⟩_`（`Star4`），即 `x ≡˘⟨ y≡x ⟩ y≡z`，不要再用已删除的 `_≡⟨_⟨_`

### where 子句

局部定义使用 `where`：`where` 与主证明体左对齐（通常 2 空格）；`where` 内的每个名字再缩进 2 空格，其定义体再缩进 2 空格：

```agda
*4·78 : ⊢ (((p ⊃ q) ∨ (p ⊃ r)) ≡ (p ⊃ (q ∨ r)))
*4·78 =
  _ ≡⟨ ... ⟩
  ...
  _ ∎
  where
    inner =
      _ ≡⟨ ... ⟩
      _ ∎
```

### MP / Syll / Comp 等规则

左结合的连续三段论写成 `Syll₃` / `Syll₄`（定义在 `Star2`），避免 `Syll (Syll …) …` 套娃。右结合的 `Syll a (Syll b c)` 与 `Syll₃ a b c` 在证明项上通常不同，除非恰好与左结合形式一致，否则不强行改写。

对左结合的三重断言合取 `((p ∙ q) ∙ r)`，使用 `Adj₃`（`Star4`），不要手写 `Adj (Adj …) …`。`ExpImp`（`Adj *3·3 *3·31`，∗4·87 第一合取支，PM *4·87₁）在 `Star5` 中直接使用。

由两个等价 `⊢ (p ≡ r)`、`⊢ (q ≡ s)` 经 ∗4·39 得到 `⊢ ((p ∨ q) ≡ (r ∨ s))` 时，用规则形式 `Or≡`（`Star4`），不要写 `MP (Adj …) *4·39`。

由两个等价经 ∗4·38 得到 `⊢ ((p ∙ q) ≡ (r ∙ s))` 时，用规则形式 `And≡`（`Star4`），不要写 `MP (Adj …) *4·38`。

由 `⊢ (p ≡ q)` 经 ∗4·21 得 `⊢ (q ≡ p)` 时写 `≡sym`；经 ∗4·11 得 `⊢ (~ p ≡ ~ q)` 时写 `¬≡`。不要写 `MP … *4·21→` / `MP … *4·11→`（`MP₃ … *4·13 *4·21→ …` 等链式用法除外，第二项仍是定理 `*4·21→`）。

等值链里「先对称一步再与下一步传递」时写 `_≡˘⟨_⟩_`（即 `_ ≡˘⟨ y≡x ⟩ …`），不要写 `_ ≡⟨ ≡sym … ⟩ …`。`≡sym` 仍用于非链式组合（如 `MP` / `Adj` / `Or≡` 内、`≡sym (…)` 包住整段链等）。

对 ∗1·6（`Sum`）与同一引理的 ∗2·05（`Star2` 中 `*2·05 = Sum`）的规则用法：`MP qr Sum` 与 `MP qr *2·05` 一律写成 `SumR qr`（`SumR` 定义在 `Syll` 之前，故 `Syll` 内层为 `MP pq (SumR qr)`）。

由 `*3·44` 作情形分解时，用 `Cases f g`，不要写 `MP (Adj f g) *3·44`（`Star4`）。

顶层调用换行时，第一个参数与规则名同行或单独一行，后续参数各占一行：

```agda
-- 风格 A：参数较短时单行
*5·14 : ⊢ ((p ⊃ q) ∨ (q ⊃ r))
*5·14 = MP (Syll₃ *4·61→ (*3·27) *2·21) *2·54

-- 风格 B：参数较长时换行
*5·16 : ⊢ (~ ((p ≡ q) ∙ (p ≡ ~ q)))
*5·16 =
  MP *3·24
    (TranspR (Comp (...) (...)))
```

## 命名与注释

- PM 引用：`*4·2`、`*4·21→`、`*4·41←` 等保持与 PM 一致
- 辅助引理：`*5·74′` 等
- 注释：`-- Dem. [*2·16.*2·17]` 放在定理声明之前

## 避免的写法

- 混用 2 空格与 4 空格缩进
- 过长的单行（> 100 字符）
- 等值证明中本可用 `_≡⟨_⟩_` 却写成冗长的 `Adj`/`MP` 链

### `MP (Adj …) *4·22` 与传递性

∗4·22 的一步传递 `MP (Adj h₁ h₂) *4·22` 与 `_ ≡⟨ h₁ ⟩ _ ≡⟨ h₂ ⟩ _ ∎` 等价（见 `Star4` 中 `_≡⟨_⟩_`）。能拆成「两个 `⊢ (p≡q)` / `⊢ (q≡r)`」时应用链式写法，不要用外层 `MP … *4·22` 包一层。

若两前提经 `MP₂`、∗4·86、∗4·37 等组合后，中间命题在类型上不能作为同一条链的两步（例如 ∗5·7），则保留 `MP (Adj …) *4·22`；可把 `MP` 与 `*4·22` 分行以缩短行长。
